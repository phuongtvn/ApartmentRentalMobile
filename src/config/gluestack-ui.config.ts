/**
 * Custom Gluestack UI Configuration
 * 
 * This is a placeholder configuration.
 * The actual theme configuration will be applied via the GluestackUIProvider.
 */

// For now, we'll use default configuration
export const config = {};

export type Config = typeof config;
